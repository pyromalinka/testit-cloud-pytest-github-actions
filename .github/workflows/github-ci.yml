name: Pytest

on:
  repository_dispatch:
    types: [run-tests]
  push:
    branches: [ main]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v3
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
  
    
    - name: Run filtered tests
      run: |
        pytest \
          --tmsConfigurationId=$CONFIGURATION_IDS \
          --tmsProjectId=$PROJECT_ID \
          --tmsPrivateToken=${{ secrets.TMS_PRIVATE_TOKEN }} \
          --tmsUrl=${{ secrets.TMS_URL }} \
          --tmsTestRunId=bc48af7e-a519-44f3-80a4-cc1e9236146e \
          --tmsAdapterMode=1 \
          --tmsCertValidation=false \
          --testit
